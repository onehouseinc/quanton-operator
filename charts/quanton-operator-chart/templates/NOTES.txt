Quanton Operator has been successfully deployed!

Operator Namespace: {{ .Release.Namespace }}
{{- if .Values.quantonOperator.jobNamespaces }}
Job Namespaces: {{ .Values.quantonOperator.jobNamespaces | join ", " }}

IMPORTANT: Ensure the following job namespaces exist before creating Spark applications:
{{- range .Values.quantonOperator.jobNamespaces }}
  - {{ . }}
{{- end }}

If any namespace doesn't exist, create it with:
  kubectl create namespace <namespace-name>
{{- end }}

Quick Commands:

1. Check deployment status:
   kubectl get pods -n {{ .Release.Namespace }}

2. View Quanton Operator logs:
   kubectl logs -n {{ .Release.Namespace }} deployment/quanton-controller

3. Verify JWT token secret:
   kubectl get secret onehouse-token -n {{ .Release.Namespace }}

4. Create a QuantonSparkApplication:
   kubectl apply -f your-spark-app.yaml -n {{ .Release.Namespace }}

For more information, visit: https://docs.onehouse.ai/quanton
